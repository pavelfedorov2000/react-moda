import { Switch } from "react-router-dom";

const initialState = {
    category: null,
    sortBy: {
        type: 'popular',
        order: 'desc',
    },
};

/* const filters = (state = initialState, action) => {

} */

//export default filters;